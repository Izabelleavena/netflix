

/* function montaListagemExibicao (){
    var listagemExibir = [];
    let totalPaginas = Math.ceil(this.listagem.length / parseInt(this.porPagina));
    let itemInicial = (parseInt(this.paginaAtual) * parseInt(this.porPagina)) - parseInt(this.porPagina);
    let itemFinal = itemInicial + parseInt(this.porPagina);
    if(this.paginaAtual === totalPaginas){
        itemFinal = this.listagem.length;
    }
    if(this.paginaAtual <= totalPaginas){
        for(let i = itemInicial; i < itemFinal; i++){
            this.listagemExibir.push(this.listagem[i]);
            itemInicial++;
        }
    }
}*/

var primeira Foto = document.get

function mudarImagem ()